# Dependencies
node_modules
.pnpm-store

# Production build
dist
dist-ssr

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Python
__pycache__/
*.pyc

# Scheduled scraper sweep logs (local only, see backend/app/scraper/run_all_scrapers.py)
backend/logs/

# Raw source documents (copyrighted airline tariff PDFs - never public, see CLAUDE.md)
data/source_documents/tariff_sheets_raw/

# Local reference docs (kept out of git so commit history stays self-contained for teammates)
/CLAUDE.md
/PLAN.md

# Editor & OS files
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vscode
.idea
jevons_engine\run_engine.log